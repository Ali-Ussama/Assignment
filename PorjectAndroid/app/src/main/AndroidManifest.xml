<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aliosama.porjectandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Activities.Teacher.THomeActivity" />
        <activity android:name=".Activities.Activities.Teacher.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Activities.Teacher.SignUpActivity" />
        <activity android:name=".Activities.Activities.Teacher.AddCourseActivity" />
        <activity android:name=".Activities.Activities.Teacher.AddAssignmentActivity" />
        <activity android:name=".Activities.Activities.Teacher.TCourseAssignmentsActivity" />
        <activity android:name=".Activities.Activities.Teacher.UpdateCourseActivity"></activity>
    </application>

</manifest>